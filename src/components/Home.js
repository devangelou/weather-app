import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>Welcome to the Weather App</h1>
            <p>Use the navigation menu to check the weather.</p>
        </div>
    );
};

export default Home;
